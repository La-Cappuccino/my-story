import { Desktop } from '@/components/os';

export const metadata = {
  title: 'AllanOS | AI-Powered Portfolio',
  description: 'An AI-powered desktop interface showcasing Allan Kisuule\'s portfolio — proof that we can build intelligent OS-style UIs.',
};

export default function OSPage() {
  return <Desktop />;
}
